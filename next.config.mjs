/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.pravatar.cc",
      "hebbkx1anhila5yf.public.blob.vercel-storage.com",
    ], // Add the domain here
  },
};

export default nextConfig;
